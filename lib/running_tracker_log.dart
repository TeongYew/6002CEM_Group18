import 'package:flutter/material.dart';

class RunningTrackerLog extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Log Page'),
      ),
      body: Center(
        child: Text('This is the log page.'),
      ),
    );
  }
}